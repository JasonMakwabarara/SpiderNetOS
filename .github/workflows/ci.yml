name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend-lint:
    name: backend — lint + static analysis
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          tools: composer:v2, cs2pr
          coverage: none
      - name: Cache composer
        uses: actions/cache@v4
        with:
          path: backend/vendor
          key: composer-${{ runner.os }}-${{ hashFiles('backend/composer.lock') }}
      - run: composer install --prefer-dist --no-progress --no-interaction
      - name: Pint (code style)
        run: vendor/bin/pint --test --format=checkstyle | cs2pr

  cockpit-build:
    name: cockpit — lint + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cockpit
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: cockpit/package-lock.json
      - run: npm ci --prefer-offline --no-audit
      - name: Type check / lint (best-effort; non-blocking until configured)
        run: |
          if npm run | grep -q 'lint'; then npm run lint; else echo "No lint script"; fi
      - name: Build
        run: npm run build

  python-lint:
    name: python planes — ruff
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        project: [intelligence, inference]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install ruff
        run: pip install ruff==0.6.9
      - name: Ruff check
        working-directory: ${{ matrix.project }}
        run: ruff check . --output-format=github
