# Fix: Cockpit Vue build failure — wrong API import path

## Problem
`cockpit/src/views/financial/FinancialDashboard.vue:126` imports from `'../services/api.js'` but the actual file is at `cockpit/src/services/api.js`. Since the view is nested 2 levels deep (`views/financial/`), the import should be `'../../services/api.js'`.

All other financial views (FinancialLedger, Invoices, Payments, Portfolios) use the correct path `../../services/api.js`.

## Steps
1. Edit `cockpit/src/views/financial/FinancialDashboard.vue` line 126
2. Change `'../services/api.js'` → `'../../services/api.js'`
3. Done — no other changes needed