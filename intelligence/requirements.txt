redis>=5.0,<6.0
asyncpg>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.0

# business-launch service (intelligence/services/business_launch): deterministic
# finance model workbook, docgen templates and jurisdiction YAML. python-docx,
# weasyprint and pandoc stay optional and are never imported at import time.
openpyxl>=3.1,<4.0
pyyaml>=6.0
jinja2>=3.1
