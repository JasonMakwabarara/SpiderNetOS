{
  "_comment": "Canonical schema contract for usage_daily_aggregates table. Checked by SchemaContractTest.",
  "table": "usage_daily_aggregates",
  "columns": {
    "id":            { "type": "bigint",  "nullable": false },
    "tenant_id":     { "type": "uuid",    "nullable": false },
    "date":          { "type": "date",    "nullable": false },
    "resource_type": { "type": "varchar", "nullable": false },
    "total_calls":   { "type": "integer", "nullable": false, "default": "0" },
    "total_tokens":  { "type": "integer", "nullable": false, "default": "0" },
    "total_cost":    { "type": "numeric", "nullable": false, "default": "0" },
    "cost_ceiling":  { "type": "numeric", "nullable": false, "default": "0" },
    "calculated_at": { "type": "timestamp", "nullable": false }
  },
  "absent_columns": ["request_count", "updated_at"],
  "indexes": [
    { "columns": ["tenant_id", "date", "resource_type"], "unique": true }
  ]
}
