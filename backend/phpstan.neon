includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 5
    paths:
        - app
    # Pre-existing findings are frozen in phpstan-baseline.neon; new code
    # must be clean. Shrink the baseline over time, never grow it.
