{
  "name": "spidernet-os",
  "version": "3.2.0",
  "private": true,
  "description": "SpiderNet OS — Multi-tenant AI Operating System",
  "workspaces": [
    "cockpit",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "codegen": "node tools/dev/codegen.js"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
