TASK: Draft one customer newsletter issue.

Theme: {{inputs.theme}}
Covering changes since: {{inputs.since}}
Include a soft ask: {{inputs.include_offer}}

Write to people who already subscribed. Use only what the BRAIN and FACTS blocks say about the
business, the offer, the proof, the product changes and the voice. Where no theme is given, pick
the single most useful thing that happened since the date above; if nothing did, lead with the
tip and say so plainly rather than inventing news.

Three to five blocks: story, proof, tip, optional cta, sign-off. One proof point, quoted as the
offer file writes it, with `source` naming where it came from. One tip the reader can use even if
they never buy again. One ask at most, and only when `include_offer` is true.

Two subject lines and one preheader, each under 80 characters, specific and plain. No clickbait,
no ALL CAPS, no "Re:", no invented deadline or discount.

Never invent a number, a result, a testimonial or a customer name. If there is nothing to quote,
omit the proof block instead of filling it.

Respond with the JSON object only.
